# Dependencies
node_modules

# Next.js build output (next-env.d.ts is regenerated on every build)
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Environment — real env files never go to git (.env.example is fine)
.env
.env.*
!.env.example

# Tooling / OS
.DS_Store
*.log
# project uses pnpm; an accidental npm install must not commit a second lockfile
package-lock.json

# automation scaffolding from earlier sessions
.agents/
